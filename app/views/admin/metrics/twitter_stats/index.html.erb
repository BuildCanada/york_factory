<div class="page-header">
  <h1>Twitter Stats</h1>
</div>

<div class="card" style="margin-bottom: 2rem; padding: 1.25rem;">
  <h2>Import CSV</h2>
  <%= form_tag import_admin_metrics_twitter_stats_path, multipart: true, style: "display: flex; gap: 1rem; align-items: flex-end; flex-wrap: wrap;" do %>
    <div class="form-group" style="margin-bottom: 0;">
      <label for="account">Account</label>
      <select name="account" id="account" class="form-control" style="width: 200px;">
        <option value="">Select account...</option>
        <% Metrics::TwitterStat::ACCOUNTS.each do |acct| %>
          <option value="<%= acct %>" <%= "selected" if acct == @account %>><%= acct.titleize %></option>
        <% end %>
      </select>
    </div>
    <div class="form-group" style="margin-bottom: 0;">
      <label for="file">CSV File</label>
      <input type="file" name="file" id="file" accept=".csv,.tsv,.txt" class="form-control" style="width: 300px;">
    </div>
    <button type="submit" class="btn btn-primary">Upload</button>
  <% end %>
</div>

<div style="margin-bottom: 1rem; display: flex; gap: 0.5rem;">
  <a href="<%= admin_metrics_twitter_stats_path %>" class="btn btn-sm <%= 'btn-primary' if @account.blank? %>">All</a>
  <% Metrics::TwitterStat::ACCOUNTS.each do |acct| %>
    <a href="<%= admin_metrics_twitter_stats_path(account: acct) %>" class="btn btn-sm <%= 'btn-primary' if @account == acct %>"><%= acct.titleize %></a>
  <% end %>
</div>

<% if @stats.any? %>
  <table>
    <thead>
      <tr>
        <th>Account</th>
        <th>Date</th>
        <th class="text-right">Impressions</th>
        <th class="text-right">Likes</th>
        <th class="text-right">Engagements</th>
        <th class="text-right">Bookmarks</th>
        <th class="text-right">Shares</th>
        <th class="text-right">Follows</th>
        <th class="text-right">Unfollows</th>
        <th class="text-right">Replies</th>
        <th class="text-right">Reposts</th>
        <th class="text-right">Profile Visits</th>
        <th class="text-right">Posts</th>
        <th class="text-right">Video</th>
        <th class="text-right">Media</th>
        <th class="text-muted">Updated</th>
      </tr>
    </thead>
    <tbody>
      <% @stats.each do |stat| %>
        <tr>
          <td><span class="badge badge-blue"><%= stat.account.titleize %></span></td>
          <td class="mono"><%= stat.date.strftime("%Y-%m-%d") %></td>
          <td class="text-right mono"><%= number_with_delimiter(stat.impressions) %></td>
          <td class="text-right mono"><%= number_with_delimiter(stat.likes) %></td>
          <td class="text-right mono"><%= number_with_delimiter(stat.engagements) %></td>
          <td class="text-right mono"><%= number_with_delimiter(stat.bookmarks) %></td>
          <td class="text-right mono"><%= number_with_delimiter(stat.shares) %></td>
          <td class="text-right mono"><%= number_with_delimiter(stat.new_follows) %></td>
          <td class="text-right mono"><%= number_with_delimiter(stat.unfollows) %></td>
          <td class="text-right mono"><%= number_with_delimiter(stat.replies) %></td>
          <td class="text-right mono"><%= number_with_delimiter(stat.reposts) %></td>
          <td class="text-right mono"><%= number_with_delimiter(stat.profile_visits) %></td>
          <td class="text-right mono"><%= stat.create_post %></td>
          <td class="text-right mono"><%= number_with_delimiter(stat.video_views) %></td>
          <td class="text-right mono"><%= number_with_delimiter(stat.media_views) %></td>
          <td class="text-muted" style="font-size: 0.75rem;"><%= stat.updated_at.strftime("%b %d %H:%M") %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <div class="empty">No Twitter stats yet. Upload a CSV to get started.</div>
<% end %>
