<%= form_with(model: theme,
              url: theme.persisted? ? admin_trade_barriers_theme_path(theme) : admin_trade_barriers_themes_path,
              class: "card") do |f| %>
  <% if theme.errors.any? %>
    <div class="flash flash-alert">
      <% theme.errors.full_messages.each do |msg| %><p><%= msg %></p><% end %>
    </div>
  <% end %>

  <div class="form-group">
    <label>Name</label>
    <%= f.text_field :name, class: "form-control" %>
  </div>

  <div class="btn-group mt-1">
    <%= f.submit "Save", class: "btn btn-primary" %>
    <a href="<%= admin_trade_barriers_themes_path %>" class="btn">Cancel</a>
  </div>
<% end %>
